---
- name: Play to create a user
  hosts: all
  tasks:
    - name: create a user 
      user:
        name: Jesse
        shell: /bin/bash
        state: present
    - name: create a user 
      user:        
        name: Bruce
        shell: /bin/bash
        state: present
   - name: create a user 
     user:        
       name: Sunit
       shell: /bin/bash
       state: present
